var LLNode = require('./llnode.js');
var LinkedList = require('./linkedlist.js');

// constructor for list
var head = new LLNode(1);
head.next = new LLNode(2);
head.next.next = new LLNode(3);

// write a function to add a node to the list

// write a function to remove a node from the list

// write a function to detect if the list is circular (back of the list points back to the head)
